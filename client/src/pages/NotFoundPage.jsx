import React from 'react';
import Layout from '../layouts/Layout';

const NotFoundPage = () => {
    return (
        <Layout>
            <div>NOT FOUND</div>
        </Layout>
    );
};

export default NotFoundPage;
