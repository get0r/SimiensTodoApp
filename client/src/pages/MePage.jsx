import React from 'react';

import { Layout } from '../layouts';
import { Me } from '../containers';


const MePage = () => {
    return (
        <Layout>
            <Me />
        </Layout>
    );
};

export default MePage;
