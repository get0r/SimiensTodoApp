import React from 'react';
import { Layout } from '../layouts';

const MePage = () => {
    return (
        <Layout>
            <div>
                ME Page
            </div>
        </Layout>
    );
};

export default MePage;
