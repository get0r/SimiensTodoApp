import React from 'react';

const CustomForm = ({ fields, submitName }) => {
    const generateFields = () => {
        let key = 0;
        fields.map(field => (
            <div key={ key }></div>
        ));
    }
    return (

    );
};

export default CustomForm;
